@extends('layouts.app')
@section('content')
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header bg-success text-white">{{ __('KẾT QUẢ') }}</div>
                <div class="card-body">
                    {{ __('Tạo mật khẩu mới thành công! Trở lại ứng dụng') }}
                </div>
            </div>
        </div>
    </div>
</div>
@endsection
